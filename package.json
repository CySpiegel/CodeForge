{
  "name": "codeforge",
  "displayName": "CodeForge",
  "description": "A self-hosted-first coding harness extension for Visual Studio Code.",
  "version": "0.1.11",
  "publisher": "codeforge",
  "license": "UNLICENSED",
  "engines": {
    "vscode": "^1.90.0",
    "node": ">=18"
  },
  "categories": [
    "AI",
    "Programming Languages",
    "Other"
  ],
  "activationEvents": [
    "onView:codeforge.chatView",
    "onCommand:codeforge.openChat",
    "onCommand:codeforge.newSession",
    "onCommand:codeforge.showConversations",
    "onCommand:codeforge.openSettings",
    "onCommand:codeforge.askSelection",
    "onCommand:codeforge.editSelection",
    "onCommand:codeforge.explainFile",
    "onCommand:codeforge.generateTests",
    "onCommand:codeforge.fixDiagnostics",
    "onCommand:codeforge.cancel",
    "onCommand:codeforge.resetSession"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "codeforge.openChat",
        "title": "CodeForge: Open Chat",
        "category": "CodeForge"
      },
      {
        "command": "codeforge.newSession",
        "title": "New Chat",
        "category": "CodeForge",
        "icon": "$(add)"
      },
      {
        "command": "codeforge.showConversations",
        "title": "Conversations",
        "category": "CodeForge",
        "icon": "$(history)"
      },
      {
        "command": "codeforge.openSettings",
        "title": "Settings",
        "category": "CodeForge",
        "icon": "$(gear)"
      },
      {
        "command": "codeforge.askSelection",
        "title": "CodeForge: Ask About Selection",
        "category": "CodeForge"
      },
      {
        "command": "codeforge.editSelection",
        "title": "CodeForge: Edit Selection",
        "category": "CodeForge"
      },
      {
        "command": "codeforge.explainFile",
        "title": "CodeForge: Explain Current File",
        "category": "CodeForge"
      },
      {
        "command": "codeforge.generateTests",
        "title": "CodeForge: Generate Tests",
        "category": "CodeForge"
      },
      {
        "command": "codeforge.fixDiagnostics",
        "title": "CodeForge: Fix Diagnostics",
        "category": "CodeForge"
      },
      {
        "command": "codeforge.cancel",
        "title": "CodeForge: Stop Current Request",
        "category": "CodeForge"
      },
      {
        "command": "codeforge.resetSession",
        "title": "CodeForge: Reset Session",
        "category": "CodeForge"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "codeforge",
          "title": "Code Forge",
          "icon": "media/codeforge.svg"
        }
      ]
    },
    "views": {
      "codeforge": [
        {
          "id": "codeforge.chatView",
          "name": "Code Forge",
          "type": "webview"
        }
      ]
    },
    "configuration": {
      "title": "Code Forge",
      "properties": {
        "codeforge.profiles": {
          "type": "array",
          "default": [],
          "description": "OpenAI API-compatible endpoint profiles. API keys are stored separately in VS Code SecretStorage.",
          "items": {
            "type": "object",
            "required": [
              "id",
              "label",
              "baseUrl"
            ],
            "properties": {
              "id": {
                "type": "string"
              },
              "label": {
                "type": "string"
              },
              "baseUrl": {
                "type": "string"
              },
              "defaultModel": {
                "type": "string"
              },
              "apiKeySecretName": {
                "type": "string"
              },
              "extraHeaders": {
                "type": "object",
                "additionalProperties": {
                  "type": "string"
                }
              }
            }
          }
        },
        "codeforge.activeProfile": {
          "type": "string",
          "default": "openai-api-local",
          "description": "Active CodeForge OpenAI API profile ID."
        },
        "codeforge.model": {
          "type": "string",
          "default": "",
          "scope": "resource",
          "description": "Model ID to send to the active endpoint. If empty, the active profile defaultModel is used."
        },
        "codeforge.agent.mode": {
          "type": "string",
          "default": "agent",
          "scope": "resource",
          "enum": [
            "agent",
            "ask",
            "plan"
          ],
          "description": "Active CodeForge agent mode. Agent executes coding tasks with approved local actions; Ask answers with read-only context; Plan analyzes and proposes an implementation plan."
        },
        "codeforge.network.allowlist": {
          "type": "array",
          "default": [],
          "description": "Explicit endpoint origin allowlist for OpenAI API compatible backends. Saving a custom endpoint URL adds its exact origin automatically.",
          "items": {
            "type": "string"
          }
        },
        "codeforge.permissions.mode": {
          "type": "string",
          "default": "smart",
          "scope": "resource",
          "enum": [
            "manual",
            "smart",
            "fullAuto"
          ],
          "description": "Approval mode for local tools. Manual asks before edits and commands; Smart allows read/search but asks before edits, commands, memory writes, notebooks, or service calls; Full Auto proceeds without most prompts."
        },
        "codeforge.permissions.rules": {
          "type": "array",
          "default": [],
          "scope": "resource",
          "description": "Permission rules. Each rule has kind, pattern, behavior, scope, and optional description.",
          "items": {
            "type": "object",
            "required": [
              "kind",
              "pattern",
              "behavior"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "tool",
                  "path",
                  "command",
                  "endpoint"
                ]
              },
              "pattern": {
                "type": "string"
              },
              "behavior": {
                "type": "string",
                "enum": [
                  "allow",
                  "ask",
                  "deny"
                ]
              },
              "scope": {
                "type": "string",
                "enum": [
                  "workspace",
                  "user"
                ],
                "default": "workspace"
              },
              "description": {
                "type": "string"
              }
            }
          }
        },
        "codeforge.mcp.servers": {
          "type": "array",
          "default": [],
          "scope": "resource",
          "description": "Explicit MCP server configurations. CodeForge never connects to MCP servers unless configured here and requested by a tool call.",
          "items": {
            "type": "object",
            "required": [
              "id",
              "label",
              "transport"
            ],
            "properties": {
              "id": {
                "type": "string"
              },
              "label": {
                "type": "string"
              },
              "enabled": {
                "type": "boolean",
                "default": true
              },
              "transport": {
                "type": "string",
                "enum": [
                  "stdio",
                  "http",
                  "sse"
                ]
              },
              "command": {
                "type": "string"
              },
              "args": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "cwd": {
                "type": "string"
              },
              "url": {
                "type": "string"
              },
              "headers": {
                "type": "object",
                "additionalProperties": {
                  "type": "string"
                }
              }
            }
          }
        },
        "codeforge.context.maxFiles": {
          "type": "number",
          "default": 24,
          "minimum": 1,
          "maximum": 200,
          "description": "Internal safety cap for automatic repo context items.",
          "deprecationMessage": "CodeForge no longer exposes this in its settings UI. Use tool-based search/glob/read and the context window token limit."
        },
        "codeforge.context.maxTokens": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "maximum": 10000000,
          "description": "Manual context window override in tokens. Set to 0 to use the selected model context length returned by /v1/models."
        },
        "codeforge.context.maxBytes": {
          "type": "number",
          "default": 120000,
          "minimum": 8000,
          "maximum": 2000000,
          "description": "Legacy fallback context byte budget used when no token context length is configured or discovered.",
          "deprecationMessage": "Use codeforge.context.maxTokens instead."
        },
        "codeforge.commands.timeoutSeconds": {
          "type": "number",
          "default": 120,
          "minimum": 5,
          "maximum": 1800,
          "description": "Maximum execution time for approved shell commands. This does not control model response streaming."
        },
        "codeforge.requests.idleTimeoutSeconds": {
          "type": "number",
          "default": 300,
          "minimum": 30,
          "maximum": 1800,
          "description": "Maximum time CodeForge waits without model stream activity before stopping a model request."
        },
        "codeforge.requests.streamCompletionGraceSeconds": {
          "type": "number",
          "default": 30,
          "minimum": 1,
          "maximum": 120,
          "description": "Time the OpenAI adapter waits after the last streamed chunk before treating the stream as finished. Increase this for slow local models that pause between tool-call argument fragments."
        },
        "codeforge.agent.maxInvalidToolCallRetries": {
          "type": "number",
          "default": 3,
          "minimum": 0,
          "maximum": 20,
          "scope": "resource",
          "description": "How many consecutive iterations the agent loop will tolerate when the model only produces invalid (unparseable) native tool calls before stopping with an error."
        },
        "codeforge.commands.outputLimitBytes": {
          "type": "number",
          "default": 200000,
          "minimum": 16000,
          "maximum": 2000000,
          "description": "Maximum stdout and stderr bytes retained per approved shell command stream."
        },
        "codeforge.workers.maxConcurrent": {
          "type": "number",
          "default": 3,
          "minimum": 1,
          "maximum": 16,
          "scope": "resource",
          "description": "Maximum number of CodeForge sub-agents (workers) that run at once. Additional spawns queue and start as running workers finish."
        },
        "codeforge.learning.enabled": {
          "type": "boolean",
          "default": true,
          "scope": "resource",
          "description": "Let CodeForge learn from finished tasks: distill durable lessons (and reusable skills) from the fixes and code it writes, then apply the relevant ones on future tasks."
        },
        "codeforge.learning.autonomy": {
          "type": "string",
          "enum": [
            "review",
            "hybrid",
            "auto"
          ],
          "enumDescriptions": [
            "Propose everything for review: learned lessons and skills are pending until you accept them in the Learned panel.",
            "Hybrid: text lessons are saved automatically; skill files are proposed for approval.",
            "Automatic: lessons are saved silently; skill-file writes follow the active approval policy."
          ],
          "default": "review",
          "scope": "resource",
          "description": "How CodeForge stores what it learns."
        },
        "codeforge.learning.scope": {
          "type": "string",
          "enum": [
            "split",
            "repo",
            "global"
          ],
          "enumDescriptions": [
            "Split: project/codebase lessons stay in this repo; your preferences follow you across repos.",
            "Per-repo: everything learned stays scoped to this project.",
            "Global: share all learned knowledge across every project."
          ],
          "default": "split",
          "scope": "resource",
          "description": "Where learned knowledge applies."
        },
        "codeforge.learning.auditCadence": {
          "type": "number",
          "default": 15,
          "minimum": 1,
          "maximum": 1000,
          "scope": "resource",
          "description": "Run a self-audit (dedup, prune, refresh learned lessons) after this many learned tasks."
        },
        "codeforge.learning.maxLessons": {
          "type": "number",
          "default": 60,
          "minimum": 1,
          "maximum": 1000,
          "scope": "resource",
          "description": "Maximum number of learned lessons retained per scope before the self-audit prunes the least useful."
        },
        "codeforge.learning.maxLessonBytes": {
          "type": "number",
          "default": 24000,
          "minimum": 1000,
          "maximum": 500000,
          "scope": "resource",
          "description": "Maximum bytes of learned lessons injected into a request's context."
        },
        "codeforge.learning.skills.enabled": {
          "type": "boolean",
          "default": true,
          "scope": "resource",
          "description": "Allow CodeForge to propose reusable skills (.codeforge/skills) when it detects a repeated successful workflow."
        },
        "codeforge.learning.skills.minRepeats": {
          "type": "number",
          "default": 3,
          "minimum": 2,
          "maximum": 50,
          "scope": "resource",
          "description": "How many times a similar successful procedure must recur before CodeForge proposes a skill for it."
        },
        "codeforge.learning.agents.enabled": {
          "type": "boolean",
          "default": false,
          "scope": "resource",
          "description": "Allow CodeForge to propose specialized sub-agents (.codeforge/agents) when a kind of task recurs. Proposals are always review-only — they are never written without your approval, even in automatic autonomy."
        },
        "codeforge.learning.embeddings.enabled": {
          "type": "boolean",
          "default": false,
          "scope": "resource",
          "description": "Use the configured OpenAI-compatible /embeddings endpoint to rank learned lessons by semantic similarity (opt-in; many local servers do not support embeddings)."
        }
      }
    },
    "menus": {
      "view/title": [
        {
          "command": "codeforge.newSession",
          "when": "view == codeforge.chatView",
          "group": "navigation@1"
        },
        {
          "command": "codeforge.showConversations",
          "when": "view == codeforge.chatView",
          "group": "navigation@2"
        },
        {
          "command": "codeforge.openSettings",
          "when": "view == codeforge.chatView",
          "group": "navigation@3"
        }
      ],
      "editor/context": [
        {
          "command": "codeforge.askSelection",
          "when": "editorHasSelection",
          "group": "navigation@10"
        },
        {
          "command": "codeforge.editSelection",
          "when": "editorHasSelection",
          "group": "navigation@11"
        },
        {
          "command": "codeforge.generateTests",
          "when": "editorHasSelection",
          "group": "navigation@12"
        },
        {
          "command": "codeforge.fixDiagnostics",
          "group": "navigation@13"
        }
      ],
      "explorer/context": [
        {
          "command": "codeforge.explainFile",
          "group": "navigation@20"
        }
      ]
    }
  },
  "scripts": {
    "compile": "tsc -p ./tsconfig.json",
    "watch": "tsc -watch -p ./",
    "compile:tests": "tsc -p ./tsconfig.test.json",
    "test": "npm run compile:tests && node --test out-test/test/unit/*.test.js out-test/test/integration/*.test.js",
    "vscode:test": "npm run compile && npm run compile:tests && node ./out-test/test/runTest.js",
    "package": "npm run compile && vsce package --allow-missing-repository --no-dependencies"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "@types/vscode": "^1.90.0",
    "@vscode/test-electron": "^2.4.1",
    "@vscode/vsce": "^2.15.0",
    "typescript": "^5.7.2"
  }
}
